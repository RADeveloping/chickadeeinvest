@model chickadee.Models.Property

@{
    ViewData["Title"] = "Delete";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<h3 class="lead">Delete Property</h3>
<p>Are you sure you want to delete this property?</p>
<hr/>
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Address)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Address)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="PropertyId"/>
        <input type="submit" value="Delete" class="btn btn-lg btn-danger w-100 my-2"/>
        <a asp-action="Index" class="w-100 btn btn-lg btn-secondary my-2">Back to List</a>
    </form>
</div>